version: '3.4'

services:
  ayudacompra-api:
    image: ayudacompra-api
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      JAVA_OPTS: -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005,quiet=y
    ports:
      - 9000
      - 5005:5005